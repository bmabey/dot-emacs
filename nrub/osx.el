;; Lion Hack:
(ns-set-resource nil "ApplePressAndHoldEnabled" "NO")

;; Remap the Apple command key to work like the meta key
(setq mac-command-modifier 'meta)

;; Clipboard
(setq x-select-enable-clipboard t)
